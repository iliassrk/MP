import { AnimationHoverEffectDirective } from './animation-hover-effect.directive';

describe('AnimationHoverEffectDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimationHoverEffectDirective();
    expect(directive).toBeTruthy();
  });
});
